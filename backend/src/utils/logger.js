// logger.js
// Responsibility: Centralized logging helper for the coding module. Exports
// empty async wrappers for future instrumentation.

const logInfo = async (msg, meta = {}) => {
  // placeholder
};

const logError = async (msg, meta = {}) => {
  // placeholder
};

module.exports = {
  logInfo,
  logError,
};
