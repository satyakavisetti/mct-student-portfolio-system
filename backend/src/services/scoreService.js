// scoreService.js
// Responsibility: Compute platform-agnostic scores, leaderboards and ranking
// based on normalized profile data. Exports empty async functions as placeholders.

const computeScoreForProfile = async (profile) => {
  // placeholder
};

const computeLeaderboardSnapshot = async (platform, date) => {
  // placeholder
};

module.exports = {
  computeScoreForProfile,
  computeLeaderboardSnapshot,
};
